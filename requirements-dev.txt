-r requirements.txt
pytest>=8.2
pytest-cov>=5.0
httpx>=0.27
# Pinned exactly: an unpinned floor let a new ruff release expand the default
# ruleset and turn CI red. Dependabot proposes bumps as PRs that must pass CI.
ruff==0.16.0
